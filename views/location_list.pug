extends layout

block content
	h1= title
	
	ul
	each location in location_list
		li
			a(href=location.url) #{location.LocationID} - #{location.City}, #{location.State} 
			span.status Status: #{location.Status.Status}
	else 
		li There are no locations.